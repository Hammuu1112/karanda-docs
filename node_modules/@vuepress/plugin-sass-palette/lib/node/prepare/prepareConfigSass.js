import { getIdPrefix, getPath } from '../utils.js';
export const prepareConfigSass = (app, id, defaultConfig, userConfig) => app.writeTemp(`sass-palette/${getIdPrefix(id)}config.scss`, `\
@import "file:///${getPath(defaultConfig)}";
@import "file:///${getPath(userConfig)}";
`);
